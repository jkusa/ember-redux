import { getState } from 'dummy/helpers/get-state';
import { module, test } from 'qunit';

module('Unit | Helper | get state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getState([42]);
  assert.ok(result);
});
